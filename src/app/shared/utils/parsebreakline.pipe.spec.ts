import { ParsebreaklinePipe } from './parsebreakline.pipe';

describe('ParsebreaklinePipe', () => {
  it('create an instance', () => {
    const pipe = new ParsebreaklinePipe();
    expect(pipe).toBeTruthy();
  });
});
